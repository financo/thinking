复用类
	从外部来看，导出类就像一个与基类接口相同的新类。但继承不只是复制基类接口，当创建一个导出类
	对象时，该对象包含了一个基类子对象。这个子对象与你用基类直接创建的对象是一样的。二者区别在于，
	后者来自于外部，而基类子对象被包装在导出类对象内部
	1.组合语法
	2.继承语法
		初始化基类：若基类构造器没有被写成带参构造器，导出类会自动调用基类构造器
				否则，需要在导出类中通过super(arg)调用基类构造器
	3.代理
	4.结合使用组合和继承
		子类中不能使用与父类方法签名相同但返回类型不同的方法
	5.在组合与继承之间选择
		is-a用继承	has-a用组合
	6.protected关键字
		对于子类和同一包下的类具有访问权限，chapter06中有相关测试代码
	7.向上转型
		尽量用组合，避免用继承。是否需要向上转型，可作为使用继承的依据
	8.final关键字
		final数据：基本类型时，值不能改变；引用类型时，引用不能改变。
		final方法：锁定方法，防止继承类对其修改；提高jvm效率。
		final类：不可被继承
	9.初始化及类的加载
		基类静态初始化 --> 导出类静态初始化 --> 基类非静态初始化 --> 基类构造器 -->
 		导出类非静态初始化 --> 导出类构造器