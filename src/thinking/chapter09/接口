接口
	使用接口的核心原因：a.为了能够向上转型为多个基类型;b.放置客户端程序员创建该类的对象
	1.抽象类和抽象方法
		abstract修饰的类为抽象类，修饰的方法为抽象方法。创建抽象类对象，继承抽象类都必须实现抽象方法
	2.接口
		用来建立类与类之间的协议。
		接口中可以包含域，隐式定义为static和final类型
		接口中的方法须为public，即使不声明public，也会隐式声明为public
	3.完全解耦
	4.Java中的多继承
	5.通过继承来扩展接口
		实现多重继承时，若两个接口有相同的方法签名和返回值，没问题。若返回值不同编译出错
	6.适配接口
	7.接口中的域
		接口这种的域自动为public static final类型
		接口中的域不能为空final，饭可以被非常量表达式初始化
	8.接口嵌套
	9.接口与工厂
		即抽象工厂模式