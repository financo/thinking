一切皆对象
	1.用引用操纵对象
	2.必须由你常见所有对象
		对象存储的地方
			a.寄存器
			b.堆栈（存储对象引用，或者指向对象引用的句柄）
			c.堆（存储对象的地方）
			d.常量存储
			e.非RAM存储
		基本类型
			基本类型很小很简单，用new在堆中创建对象效率不高，基本类型的创建是非引用的“自动”变量，这个变量直接存值
	3.永远不需要销毁对象
		Java对象不具备和基本类型一样的生命周期，它可以存活于作用域之外，但超出作用域不能访问
	4.创建新的数据类型：类
		类中可以设置两种类型的元素：字段（数据成员），方法。
	5.方法，参数，返回值
		方法：名称，参数，返回值，方法体
		方法名和参数列表合称方法签名，用于唯一标识某个方法
	6.构建一个Java程序
		通过import导入其他模块，使其他模块中的类在本类中可见
		static关键字用于创建类数据，类方法。类数据，类方法作为整个类，而不是某个特定对象而存在的
	7.你的第一个Java程序
	8.注释和嵌入式文档
		/*这里是注释的内容*/
		//这里是注释的内容
		/**
		 *这种格式是用来写注释文档的
		 */
	9.编码风格
		类名首字母大写，方法，字段首字母小写；驼峰风格